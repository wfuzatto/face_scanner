services:
  face-scanner:
    build: .
    container_name: face-scanner
    restart: unless-stopped
    env_file:
      - .env
    environment:
      APP_ENV: standalone
      SESSION_DB_PATH: /app/data/face_scanner.sqlite3
      # Libera somente /dashboard-api/* para o dashboard de teste.
      # /api/v1/* continua protegido pela API_KEY do .env.
      DASHBOARD_UNAUTHENTICATED: "true"
    volumes:
      - face_scanner_data:/app/data
    # HTTP somente como upstream local para o Caddy do host.
    # O launcher HTTPS exporta STANDALONE_PORT dinamicamente quando necessário.
    ports:
      - "${STANDALONE_BIND:-127.0.0.1}:${STANDALONE_PORT:-18092}:8091"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8091/api/v1/health', timeout=4)"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    security_opt:
      - no-new-privileges:true

volumes:
  face_scanner_data:
